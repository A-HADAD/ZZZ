import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sysops.terminal',
  appName: 'SysOps Terminal',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
